spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ebanking-rechargedb
    username: root
    password: 'root123'
  jpa:
    hibernate:
      ddl-auto: update

server:
  address: 0.0.0.0
  port: 8080

account-service:
  url: http://localhost:8081
